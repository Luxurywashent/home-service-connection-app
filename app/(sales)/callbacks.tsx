/**
 * Sales Rep - Callbacks Tab
 * Re-exports the admin callbacks screen so sales reps can manage
 * their callback list directly from the sales layout.
 */
export { default } from "../(tabs)/admin-callbacks";
