/**
 * Sales Rep - Book Tab
 * Re-exports the full admin schedule/booking screen so sales reps can
 * view all city calendars and book appointments exactly like admins.
 */
export { default } from "../(tabs)/admin-schedule";
